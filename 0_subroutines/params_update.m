function PAR=params_update(XTAL,EXP,INFO,PLOT,DATA,VECS);
% PAR=params_update(XTAL,EXP,INFO,PLOT,DATA,VECS);
% 	Updates PAR, used before passing to a subroutine or passing back to parent

PAR.XTAL=XTAL;
PAR.INFO=INFO;
PAR.EXP=EXP;
PAR.PLOT=PLOT;
PAR.DATA=DATA;
PAR.VECS=VECS;

